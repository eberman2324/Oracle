####################################################################
##### Run CROSSCHECK command, then delete expired backups.             
####################################################################
exit;
crosscheck backup;
   delete expired backup of database;

