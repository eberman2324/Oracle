# listener.ora Network Configuration File: /orahome/u01/app/oracle/product/12.1.0.2/grid/network/admin/listener.ora
# Generated by Oracle configuration tools.

{{ db_name }} =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = IPC)(KEY = {{db_name}}_IPC))
      (ADDRESS = (PROTOCOL = TCP)(HOST = {{ ansible_hostname }})(PORT = {{ db_port }}))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC{{ db_port }}))
    )
  )

ENABLE_GLOBAL_DYNAMIC_ENDPOINT_{{ db_name }}=ON		# line added by Agent
VALID_NODE_CHECKING_REGISTRATION_{{ db_name }}=SUBNET		# line added by Agent

SECURE_REGISTER_{{ db_name }} = (IPC)

SID_LIST_{{ db_name }} =
  (SID_LIST =
    (SID_DESC =
      (ORACLE_HOME = {{ oracle_home }})
      (SID_NAME = {{ db_name }}) 
    )
  )
