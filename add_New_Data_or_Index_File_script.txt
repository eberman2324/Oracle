!! Always check make sure you have ASM DISK space to add new files 

**********************************************************

select
  MOUNT_STATUS     ,
  HEADER_STATUS ,
  TOTAL_MB ,
  FREE_MB  ,
  substr(NAME,1,20) "Name",
  substr(LABEL,1,10) "Label",
  substr(PATH,1,50) "Path"
from
        v$asm_disk
order by
        path,
        name
;


select 
  substr(a.group_number,1,3) "Group Num", 
  a.name "Group Name",
  substr(b.disk_number,1,3) "Disk Num", 
  b.name "Disk Name", 
  b.MOUNT_STATUS "Disk Mount Status",
  b.HEADER_STATUS "Disk Header Status",
  b.TOTAL_MB, 
  b.FREE_MB, 
  substr(b.PATH,1,50) "Path"
from v$asm_diskgroup a, v$asm_disk b
    where a.group_number = b.group_number
    order by a.group_number,b.disk_number;

*******************************************************


xhedwdbm1p.aetna.com (oracle) HEDWPRD


--Add Index File

cd /home/oracle/tls/space

## specify in the file how many files to be added
## do not do more than 3

vi NEW_INDEX.sql

## It will ask for # of files should be same # as above
./add_space_to_index_tablespace.sh


Login to standby to make sure files made it

+ASM
asmcmd

lsdg

--Add Data File

cd /home/oracle/tls/space

## specify in the file how many files to be added
## do not do more than 3

vi NEW_DATA.sql

## It will ask for # of files should be same # as above
./add_space_to_data_tablespace.sh

Login to standby to make sure files made it

+ASM
asmcmd

lsdg


xhepydbm1p (oracle) HEPYPRD

 
--Add Index File

cd /home/oracle/tls/space

## specify in the file how many files to be added
## do not do more than 3

vi NEW_INDEX.sql

## It will ask for # of files should be same # as above
./add_space_to_index_tablespace.sh


Login to standby to make sure files made it

+ASM
asmcmd

lsdg

--Add Data File

cd /home/oracle/tls/space

## specify in the file how many files to be added
## do not do more than 3

vi NEW_DATA.sql

## It will ask for # of files should be same # as above
./add_space_to_data_tablespace.sh



Login to standby to make sure files made it

+ASM
asmcmd

lsdg


